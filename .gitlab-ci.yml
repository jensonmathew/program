#### see https://docs.gitlab.com/ce/ci/yaml/README.html for all available options

# you can delete this line if you're not using Docker
image: busybox:latest
   
build1:
  stage: build
  script:
    - echo "Do your build here"
    - echo $CI_JOB_ID
    - echo $SECRET
    
    
# Example of building a docker container and publishing to project's gitlab docker registry

# Example of using the docker container from the example above for a build job, and showing how different docker containers can be done per job.

# Example of feature flags being set and environments being used with CD
